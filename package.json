{
  "name": "blog-ecommerce",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"hugo server -D\"",
    "build": "tinacms build && hugo",
    "tina-dev": "tinacms dev",
    "tina-build": "tinacms build"
  },
  "dependencies": {
    "@tinacms/auth": "^1.0.3",
    "@tinacms/cli": "^1.5.29",
    "tinacms": "^1.5.20"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^4.9.5"
  }
}